#!/usr/bin/env bash
# -*- encoding: utf-8 -*-

if [ $TMUX ]; then
	tmux splitw -h -p30
	tmux neww -n 'vim' "vim $1"
else
	tmux new -d
	tmux splitw -h -p30
	tmux selectp -t 0
	tmux neww -n 'vim' "vim $1"
	tmux -2 a
fi
