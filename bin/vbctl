#!/usr/bin/env bash
# -*- encoding: utf-8 -*-

if [ $# = 0 ]; then
  vboxmanage startvm l-vm

elif [ "$1" = 'nog' ]; then
  vboxmanage startvm l-vm --type headless

elif [ "$1" = 'off' ]; then
  vboxmanage controlvm l-vm poweroff
fi
